<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <Button
        android:id="@+id/btnBuildings"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Buildings"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="16dp"
        android:layout_alignParentTop="true" />

    <Button
        android:id="@+id/btnSpaceships"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Spaceships"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/btnBuildings"
        android:layout_marginTop="16dp" />

    <Button
        android:id="@+id/btnResearch"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Research"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/btnSpaceships"
        android:layout_marginTop="16dp" />

    <Button
        android:id="@+id/btnHome"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Home"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/btnResearch"
        android:layout_marginTop="16dp" />
</RelativeLayout>
